name: FAZ 3D4 corrected residual support

on:
  pull_request:
    paths:
      - ".github/workflows/faz-3d4-residual-exact-v3.yml"

permissions:
  contents: write

concurrency:
  group: faz-3d4-residual-exact-v3-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  verify-and-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      PYTHONHASHSEED: "0"
      PYTHONPATH: engine:.
      VEKTORYUM_WORKERS: "1"
      TEST_BRANCH: agent/faz3d4-residual-test
      TARGET_BRANCH: agent/rfv-3d3-canvas-independent-alpha-mask
      BASE_PATCH_SHA: 6e80719f58b1f2b978157448cd15b4e5f4581ae099a6d519224ee50a37144b2d
      CORRECTION_SHA: 1a1046eecbd3c55f14bb8723270893a5c455898f85f891f79df56ea26603a536
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_BRANCH }}
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: engine/requirements.txt
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends ghostscript libcairo2
          python -m pip install -q -r engine/requirements.txt pytest httpx
      - name: Apply exact base and correction patches
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin "${TEST_BRANCH}:refs/remotes/origin/${TEST_BRANCH}"
          git show "origin/${TEST_BRANCH}:engine/regression/residual_exact_00.txt" | head -c 9000 > /tmp/base00
          git show "origin/${TEST_BRANCH}:engine/regression/residual_exact_01.txt" | head -c 9000 > /tmp/base01
          git show "origin/${TEST_BRANCH}:engine/regression/residual_exact_02.txt" | head -c 9000 > /tmp/base02
          git show "origin/${TEST_BRANCH}:engine/regression/residual_exact_03.txt" | head -c 6800 > /tmp/base03
          cat /tmp/base00 /tmp/base01 /tmp/base02 /tmp/base03 > /tmp/base.b64
          test "$(wc -c < /tmp/base.b64)" -eq 33800
          base64 --decode /tmp/base.b64 > /tmp/base.patch
          test "$(sha256sum /tmp/base.patch | cut -d' ' -f1)" = "${BASE_PATCH_SHA}"
          git apply --check /tmp/base.patch
          git apply /tmp/base.patch

          git show "origin/${TEST_BRANCH}:engine/regression/residual_corr_00.txt" | head -c 6000 > /tmp/corr00
          git show "origin/${TEST_BRANCH}:engine/regression/residual_corr_01.txt" | head -c 6000 > /tmp/corr01
          git show "origin/${TEST_BRANCH}:engine/regression/residual_corr_02.txt" | head -c 252 > /tmp/corr02
          cat /tmp/corr00 /tmp/corr01 /tmp/corr02 > /tmp/correction.b64
          test "$(wc -c < /tmp/correction.b64)" -eq 12252
          base64 --decode /tmp/correction.b64 > /tmp/correction.patch
          test "$(sha256sum /tmp/correction.patch | cut -d' ' -f1)" = "${CORRECTION_SHA}"
          git apply --check /tmp/correction.patch
          git apply /tmp/correction.patch
          git diff --check
          python -m py_compile engine/app/alpha_candidate_painter.py engine/app/alpha_candidate_support.py engine/test_alpha_painter_residual_support.py
      - name: Run focused residual contracts
        shell: bash
        run: |
          set -o pipefail
          python -m unittest engine.test_alpha_painter_residual_support engine.test_alpha_painter_encoding engine.test_alpha_painter_ledger engine.test_alpha_painter_retry engine.test_alpha_painter_reconstruction_fidelity engine.test_alpha_painter_stroke_continuation -v 2>&1 | tee faz3d4-residual-focused.log
      - name: Run complete alpha suite
        shell: bash
        run: |
          set -o pipefail
          python -m unittest engine.test_alpha_candidate_identity engine.test_alpha_candidate_knockout engine.test_alpha_candidate_paint_selection engine.test_alpha_candidate_support engine.test_alpha_candidate_validation engine.test_alpha_candidate_painter engine.test_alpha_candidate_background engine.test_alpha_mask_adaptive engine.test_alpha_mask_transaction engine.test_alpha_preprocess engine.test_alpha_noop_preflight engine.test_alpha_clip_encoding engine.test_alpha_artwork_identity engine.test_alpha_painter_encoding engine.test_alpha_painter_ledger engine.test_alpha_painter_retry engine.test_alpha_painter_reconstruction_fidelity engine.test_alpha_painter_stroke_continuation engine.test_alpha_painter_residual_support -v 2>&1 | tee faz3d4-residual-alpha.log
      - name: Require Class Reklam and gradient acceptance
        working-directory: engine
        shell: bash
        run: |
          set -euo pipefail
          python test_visual_regression.py --case class_reklam --case gradient_logo --require-baseline --out-dir regression/faz3d4-residual-v3 > regression/faz3d4-residual-v3.log 2>&1
          cat regression/faz3d4-residual-v3.log
          grep -F "PASS class_reklam" regression/faz3d4-residual-v3.log
          grep -F "PASS gradient_logo" regression/faz3d4-residual-v3.log
      - name: Commit and push accepted residual support
        shell: bash
        run: |
          set -euo pipefail
          git add engine/app/alpha_candidate_painter.py engine/app/alpha_candidate_support.py engine/test_alpha_painter_residual_support.py
          git -c user.name='Vektoryum CI' -c user.email='actions@github.com' commit -m 'fix: clip painter support to measured coverage deficit' -m 'Compose canvas, residual-clipped support and original artwork once, then apply source alpha once. Preserve one artwork geometry tree, comparison underpaint, artwork fingerprint, vector-only output, unchanged thresholds, TransformJournal, retry policy, stroke list, corpus, benchmark, repeat, shards and timeout.'
          rm -rf engine/regression/faz3d4-residual-v3
          rm -f faz3d4-residual-focused.log faz3d4-residual-alpha.log engine/regression/faz3d4-residual-v3.log
          test -z "$(git status --porcelain --untracked-files=no)"
          git push origin "HEAD:${TARGET_BRANCH}"
      - name: Upload safe scalar logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: faz3d4-residual-v3-${{ github.run_id }}
          path: |
            faz3d4-residual-focused.log
            faz3d4-residual-alpha.log
            engine/regression/faz3d4-residual-v3.log
          if-no-files-found: warn
          retention-days: 7
